from django.contrib import admin
from marketplace_app.models import Product, Purchase, Users

# Register your models here.
admin. site. register(Product)
admin. site. register(Purchase)
admin. site. register(Users)